{
  "name": "monorepo-profiles",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "engines": { "node": ">=20" },
  "scripts": {
    "test": "node --test --test-reporter=spec 'test/**/*.test.mjs'"
  }
}
