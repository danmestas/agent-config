# Commands

Slash commands referenced by outfits, modes, or accessories via their
`include.commands` block. Each command: `commands/<name>/COMMAND.md`.

Current entries:

- `career-interview-setup/` — configures the career-interview TTS plugin.
- `monorepo-profile/` — `/profile` slash command for monorepo-profiles.
