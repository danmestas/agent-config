# Accessories

Small, named, repeatable add-ons applied via `suit ... --accessory <name>`.

Each accessory is a directory: `accessories/<name>/accessory.md` with
frontmatter declaring an `include:` block.

See suit ADR-0010 for resolution semantics.

(empty for now — first accessories will be added in follow-up PRs)
